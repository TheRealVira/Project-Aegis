apiVersion: v1
kind: Service
metadata:
  name: {{ include "example2-service.fullname" . }}
  labels:
    {{- include "example2-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example2-service.selectorLabels" . | nindent 4 }}
