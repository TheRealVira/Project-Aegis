apiVersion: v1
kind: Service
metadata:
  name: {{ include "example1-service.fullname" . }}
  labels:
    {{- include "example1-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "example1-service.selectorLabels" . | nindent 4 }}
